
<div class="container">
<h1 class="center">Open Permits</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @permits.each do |permit| %>
      <tr>
        <td><%= permit.issued %></td>
        <td><%= link_to '#{permit.id}', permit %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>